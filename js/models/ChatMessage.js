class ChatMessage {
    constructor(id, targetUserId, senderId, text, date) {
        this.id = id;
        this.targetUserId = targetUserId;
        this.senderId = senderId;
        this.text = text;
        this.date = date;
    }
}